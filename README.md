# dotfiles

```
pacman -S stow

git clone dotfiles to ~

stow <dir> - creates symlink based on provided layout

stow -R --adopt <dir> - replaces <dir> with symlink based on provided layout

stow -t / <dir> - creates symlink in a specified path -> /
```
